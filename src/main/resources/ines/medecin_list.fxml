<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ListView?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.effect.DropShadow?>
<?import javafx.scene.paint.Color?>
<?import javafx.scene.text.Font?>

<AnchorPane xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="controllers.ines.MedecinController"
            prefWidth="800" prefHeight="600"
            style="-fx-background-color: linear-gradient(to bottom, #f4f4f4, #e0e0e0);">

    <!-- Utilisation d'un VBox pour un affichage fluide -->
    <VBox layoutX="0" layoutY="0" prefWidth="800" prefHeight="600"
          spacing="20" alignment="CENTER"
          style="-fx-padding: 20px;"
          AnchorPane.topAnchor="0.0" AnchorPane.bottomAnchor="0.0"
          AnchorPane.leftAnchor="0.0" AnchorPane.rightAnchor="0.0">

        <!-- Titre en haut -->
        <Label text="Choisissez votre médecin"
               style="-fx-font-size: 26px;
                      -fx-font-weight: bold;
                      -fx-text-fill: #007bff;
                      -fx-padding: 10px;
                      -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 10, 0, 2, 2);"/>

        <!-- Liste des médecins -->
        <ListView fx:id="medecinListView" prefWidth="750" prefHeight="450"
                  style="-fx-background-color: white;
                         -fx-border-color: #007bff;
                         -fx-border-radius: 10px;
                         -fx-border-width: 2px;
                         -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 10, 0, 2, 2);
                         -fx-font-size: 14px;
                         -fx-padding: 10px;">
        </ListView>

    </VBox>

</AnchorPane>